=========================
OpenContrail Docker image
=========================

This repository is used to build OpenContrail docker images using Artifactory as
caching proxy for dependencies and support for ConfigMap in entrypoint.
